"use strict"

let x = 7;

x = x % 3

console.log(x);
/* result is 1 because of remainder operator (%) returns the remainder left over when one operand is divided by a second operand.
(7/3=2,... not devide without remainder)3*2 + 1 =7, remainder is 1
*/
let y = 21;

y = y % 4

console.log(y);
/* result is 1 because of remainder operator (%) returns the remainder left over when one operand is divided by a second operand.
(21/4=5,...not devide without remainder)4*5 + 1 =21, remainder is 1
*/

let z = 13;

z = z % 2

console.log(z);
/* result is 1 because of remainder operator (%) returns the remainder left over when one operand is divided by a second operand.
(13/2=6,... not devide without remainder)2*6 + 1 =13, remainder is 1
*/