'use strict'

    // for loop
for (let i = 0; i <= 20; i++) {
    // check up if the value of the counter variable is odd or even
    if (i === 0){
        console.log('It is' + ' ' + 0);
    } else  {
        (i % 2 === 0 ? console.log('The number' + ' ' + [i] + ' ' + 'is even!') : console.log('The number' + ' ' + [i] + ' ' + 'is odd!'))
    }
    
};